eureka:
    client:
        fetch-registry: true
        register-with-eureka: true
        serviceUrl:
            defaultZone: http://localhost:8084/eureka
    instance:
        prefer-ip-address: true
server:
    port: 8081
spring:
    application:
        name: USER-SERVICE
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        password: Pinu@1997
        url: jdbc:mysql://localhost:3306/movieratinuser
        username: root
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dilect: org.hibernate.dialect.MySQL8Dialect